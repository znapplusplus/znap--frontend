// No PostCSS plugins needed — CSS variables + CSS modules handle everything.
// Keeping this file empty (instead of deleting) because it was created earlier.
const config = {
  plugins: [],
};
export default config;
